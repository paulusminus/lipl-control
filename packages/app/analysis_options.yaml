include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - lib/l10n/arb/**.dart
    - build/**
    - android/**
    - web/**
    - linux/**

linter:
  rules:
    - invalid_runtime_check_with_js_interop_types
